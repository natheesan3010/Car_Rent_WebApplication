@{
    var role = Context.Session.GetString("Role");

    // if (role == "Admin")
    // {
    //     Layout = "~/Views/Shared/_AdminLayout.cshtml";
    // }
    if (role == "Customer")
    {
        Layout = "~/Views/Shared/_CustomerLayout.cshtml";
    }
    else
    {
        Layout = "~/Views/Shared/_GuestLayout.cshtml"; // default for guests
    }
}
